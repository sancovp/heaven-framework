# vision: Persona_Force_Absolute (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-07-09T04:12:31  OPEN: OPEN INVESTIGATION (Isaac 2026-07-09, UNRESOLVED — hit context wall, compacting): Isaac says we PREVIOUSLY implemented persona_force vs persona_force_absolute + a four-layer devdir split + walk-up-until-the-highest-.devdir, and it MUST BE IN HEAVEN because it is about how the SYSTEM PROMPT RESOLVES (only heaven builds system prompts). I could not find that implementation. WHAT I FOUND: (1) CartON Skillmanager_Devdir_System = DESIGN DECISION 2026-06-29..30 + 07-04: replace one-.claude-per-dir with FOUR layers -- .claude (portable AIOS substrate), .heaven (interpretation), .cave (organs+metahooks), .starsystem (private loadouts+SOMA vaulting); persona DECLARED in a dir CLAUDE.md via skillmanager_persona= (relative) / absolute_skillmanager_persona= (absolute) keys = the persona_force / persona_force_absolute Isaac means; intent = persona is STATED not walked-into. STATUS soma_region unvalidated = decided-intent, implementation across repos NOT verified. (2) heaven baseheavenagent.py NEVER had skillmanager_persona/persona_force/absolute (verified: the straggler-reconciliation commit d38f80db removed ZERO lines with persona/devdir/skillmanager/force/absolute tokens -- it ADDED devdir_resolver_regression.py from upstream 0.1.31; so my rsync --delete reconciliation did NOT clobber persona work). (3) skillmanager_persona tokens DO appear in monorepo git history commits 8e6c0b49 (vendor base/onionmorph), 262f4cf9 (start_sancrev), f6480be1 (sync working tree) -- BUT a current-tree grep (--include=*.py) is EMPTY, so they are in non-.py files, or onionmorph/start_sancrev only, or added-then-removed. onionmorph is ALSO the one place that wires register_dir_context (base/onionmorph/lib/utils.py:275). NEXT STEPS for the fresh lifetime: (a) deconfabulate the 2026-06-29..30 + 07-04 planning convos (where the four-layer + persona-declaration was decided) to find the ACTUAL implementation location + status; (b) grep base/onionmorph ALL file types + git show 8e6c0b49/262f4cf9 for the real persona_force/absolute code; (c) determine if the persona-declaration devdir resolution was ever implemented in HEAVEN resolve_devdirs or ONLY designed/only-in-onionmorph; (d) RECONCILE my heaven devdir changes THIS SESSION with it. MY CHANGES THIS SESSION (may reimplement/conflict with the previous design): HeavenAgentConfig.working_dir + AFTER_TOOL_CALL _track_active_work_dir (single current dir, swaps on leave) + _resolve_devdir_roots + two-phase _devdir_levels (skip leading gap to nearest devdir, then contiguous-up, stop at first parent without a devdir, NOT .git-bounded). RELEASED: heaven 0.1.34 = working_dir + swap-on-leave but STILL the OLD .git-walk _devdir_levels (running in the reviewer image). COMMITTED-NOT-RELEASED: cfc17e29 = the two-phase non-.git walk (awaiting reconciliation with the persona_force design before releasing 0.1.35). The correct walk per Isaac = walk up until the highest contiguous .devdir (my two-phase does this) + honor persona_force/absolute declaration keys (NOT yet implemented in heaven -- the missing piece). No backticks.  tags:[Persona_Force_Absolute]
