# vision: Devdir_Resolution (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-07-09T01:32:03  FINDING: IMPLEMENTED (2026-07-09, approved plan precious-sauteeing-bird): (1) heaven devdir loading now tracks BOTH the explicit starting dir AND the dirs the agent READS/BASHES into, fixing the os.getcwd()-only bug (cwd is the launch dir, NOT where the agent works). (2) CICD block reports now open a monorepo issue. CHANGE 1 (base/heaven-framework/heaven_base/baseheavenagent.py, all keyword-safe pydantic + no start-passing callers so multi-root refactor is safe): added HeavenAgentConfig.working_dir (Optional, None -> os.getcwd() fallback); __init__ captures self._configured_cwd = config.working_dir or os.getcwd() + self._active_work_dirs=[]; module-level _extract_tool_path(tool_name,tool_args) (path-arg keys + bash-command regex, mirrors dir_context.py, defined here to avoid circular import since dir_context imports FROM baseheavenagent); AFTER_TOOL_CALL hook self._track_active_work_dir (newest-refines, cap 8, never raises) registered in __init__; resolve_devdirs refactored to walk MULTIPLE roots via new _resolve_devdir_roots (configured cwd + active dirs + optional start, level-deduped) instead of _devdir_levels(start) -- ALL reads via getattr-with-defaults so the object.__new__ regression test still passes; DEVDIR_CONTEXT header now roots= not root=. TESTED: new test_agents/test_devdir_multiroot.py 8/8 (both roots load, content-dedup, tracker from bash+read, cap+newest-refines, never-raise, helper) + existing devdir_resolver_regression.py 2/2 still green; pip install --no-deps . + import verified working_dir/helpers on installed pkg. onionmorph untouched (it wires the separate dir_context.py itself at base/onionmorph/lib/utils.py:275). CHANGE 2: new automation/cicd-reviewer/cicd_aios/.claude/hooks/block_report_to_issue.py -- Form B register(registry) -> ON_BLOCK_REPORT handler -> gh issue create on BLOCK_REPORT_ISSUE_REPO|GITHUB_REPOSITORY with the rendered block_report_md; auto-registers via the devdir hook-loader (reviewer cwd=/cicd_aios). Guards: no-op if no gh/token, LOGS never prints, never raises. Verified it registers on ON_BLOCK_REPORT + skip-path does not raise. Added issues:write to cicd-review-on-pr.yml + cicd-pr-on-push.yml. RELEASE: bumped heaven-framework 0.1.32->0.1.33 (pyproject) + reviewer Dockerfile pin ==0.1.32->==0.1.33 + canonical-source-dirs note. NEXT: push -> absorber pypi-tag releases 0.1.33 + self-hosted build-image rebuilds reviewer -> E2E verify devdir (repo-with-.claude PR reflects its rules) + block-report (forced block -> monorepo issue). NOT yet E2E-verified on the real surface -- that is the remaining step. No backticks.  tags:[Devdir_Resolution]
- [v2]  2026-07-09T01:58:41  FINDING: PROVEN E2E ON THE REAL SURFACE (2026-07-09): BOTH features work. (1) DEVDIR read/bash tracking -- opened PR #6 on a branch carrying .claude/rules/e2e-devdir-marker.md (a repo-LOCAL rule mandating the token DEVDIR-MARKER-XYZZY-42); the reviewer (rebuilt image, heaven 0.1.33) posted a review whose FIRST line was DEVDIR-MARKER-XYZZY-42 + correctly flagged the real ZeroDivisionError -- so it adopted /repo own .claude rules (via the new AFTER_TOOL_CALL tracker) AND still reviewed properly. Before the fix it only saw /cicd_aios (cwd). (2) BLOCK-REPORT -> monorepo issue -- opened PR #7 on a branch whose .claude/rules told the reviewer to WriteBlockReportTool instead of reviewing; result: monorepo open-issues 0->1, issue #8 by app/github-actions titled CICD reviewer blocked on PR #7 with the full block-report markdown (its own completed-tasks list even says Read repo-local .claude/rules/e2e-block-trigger.md which mandated the block) -- so devdir loaded the rule AND the ON_BLOCK_REPORT hook (cicd_aios/.claude/hooks/block_report_to_issue.py, force-added past the .claude/ gitignore) fired gh issue create. The review RUN correctly shows conclusion=failure (block report => no DONE => ci_agent FATAL => red CI, by design not silently green). Cleaned all test artifacts (PR#6/#7 closed+branches deleted, issue #8 closed). RELEASE: heaven 0.1.33 live on PyPI (verified /0.1.33/json 200, mirror source has 9 devdir-symbol matches), reviewer image rebuilt via self-hosted build (ghcr versions->10). TWO SECONDARY FINDINGS FIXED: (A) MIRROR LEAK -- publish_unit.sh git-archive was mirroring <subdir>/context/journal + docs/vision + docs/mirror (doc-mirror INTERNAL dev notes) to the PUBLIC mirrors; 5 of 8 leaked (carton-mcp 271 vision + 20 doc(m) files, promptworld 240 vision, cave/heaven-framework/cicd-reviewer smaller). FIX: added those 3 excludes to publish_unit.sh (prevents future) + directly removed them from all 5 mirrors HEAD (rsync --delete cannot, since now-excluded). NOTE: git HISTORY on the mirrors still holds them -- HEAD removal is right for non-secret dev notes; a full history scrub (BFG/force-push) is a separate Isaac call, not done. (B) __version__ DRIFT -- heaven_base/__init__.py hardcoded __version__=0.1.32, stale across pyproject bumps; changed to read importlib.metadata.version(heaven-framework) so it NEVER drifts again. PyPI 0.1.33 (+ current reviewer image) cosmetically self-reports 0.1.32 in __version__ (the built artifact is immutable) but the CODE is current/correct (feature verified present); next release self-corrects -- did NOT burn a 0.1.34 release just for the string. No backticks.  tags:[Devdir_Resolution]
- [v3]  2026-07-09T02:13:47  DECISION: BEHAVIOR CORRECTION (Isaac 2026-07-09, heaven 0.1.34): devdir active-dir tracking changed from ACCUMULATE-last-8 to SINGLE-CURRENT-DIR-that-SWAPS. Isaac asked when they leave that parent the devdir injections from it go away right? -- and my 0.1.33 impl did NOT: _active_work_dirs was a rolling list of the last 8 read/bashed dirs, all injected, evicted only by the cap (not by leaving). CORRECTED to match his expectation + how a CLAUDE.md actually works (active only while you are in that dir) + dir_context.py single-active model: _active_work_dir is now a SINGLE dir (the most recent read/bash) that SWAPS on move; resolve_devdirs walks [always-on configured_cwd] + [single active dir]. Leaving dir A for dir B => A rules GONE, B rules load; cwd (the agent home/AIOS) is always-on regardless. baseheavenagent.py: _track_active_work_dir sets self._active_work_dir (swap) not list-append-cap; __init__ inits _active_work_dir=None; _resolve_devdir_roots uses the single active. Tests updated: devdir_multiroot_regression.py 8/8 incl. test_tracker_swaps_on_move_so_leaving_a_dir_drops_it (proves leave->gone) + devdir_resolver_regression.py 2/2. NOTE: the single-repo E2E marker test (PR#6) cannot DISTINGUISH swap from accumulate (during one review the agent stays in /repo, so /repo rules inject either way) -- the swap behavior is proven at UNIT level. Releasing 0.1.34 (also makes __version__ consistent now that it reads importlib.metadata). No backticks.  tags:[Devdir_Resolution]
- [v4]  2026-07-09T04:22:05  INTENT: --tag Devdirs RESUME post-compact (2026-07-09): rehydrated via DMN (11 entries convo-0 + Devdir_Resolution cohered note + design concepts Skillmanager_Devdir_System / _Persona_Declaration / Devdir_System / the 06-30 design cohered note). ACTIVE: the persona_force/persona_force_absolute code-archaeology Isaac insists is IN HEAVEN. DESIGN recovered = four-layer .claude(portable AIOS substrate)/.heaven(richer interpretation of .claude directives)/.cave(organs+metahooks)/.starsystem(private loadouts+SOMA vaulting); persona = a .heaven interpretation of CLAUDE.md declared via skillmanager_persona= (relative) and absolute_skillmanager_persona= (absolute, isolates) keys; STATUS soma_region UNVALIDATED = designed not verified-implemented. NEXT (this turn): run the confusion-to-deconfabulate-to-check loop as code-archaeology = git show 8e6c0b49/262f4cf9/f6480be1 + grep base/onionmorph ALL filetypes + check current heaven baseheavenagent MYSELF + deconfabulate the 07-04 planning convos, to determine whether persona-declaration devdir resolution was ever in heaven resolve_devdirs or only designed/only-onionmorph, THEN reconcile my session changes. HELD: do NOT release two-phase walk cfc17e29 (0.1.35) until reconciled. No backticks.  tags:[Devdir_Resolution]
- [v5]  2026-07-09T04:24:03  FINDING: --tag Devdirs DECONFABULATION VERDICT (verified MYSELF via code+git, nothing inherited, 2026-07-09): the persona_force/persona_force_absolute + four-layer devdir + walk-to-highest-.devdir was DESIGNED but NEVER IMPLEMENTED IN CODE anywhere. EVIDENCE (my own greps): (1) skillmanager_persona/absolute_skillmanager_persona/persona_force = ZERO in base/heaven-framework python (grep empty) -- confirms prior claim, heaven has NO persona-key code. (2) the tokens exist ONLY in base/onionmorph/HEAVEN-DOTDIR-SPEC.md = a SPEC doc, D9 row = ACCEPTED (Phase 2) -- see §4.5 (accepted-to-build, NOT built). (3) onionmorph python = ZERO skillmanager_persona reads; its only devdir wiring is register_dir_context(hook_registry) at onionmorph/lib/utils.py:275-276 = registers HEAVEN dir_context.py. (4) heaven dir_context.py (heaven_base/hooks/dir_context.py) reads AGENTS.md/CLAUDE.md + .claude/ + .heaven/ rules, _walk_levels + refine, single-active swaps-on-move -- but does NOT parse skillmanager_persona= keys (grep of the file: no persona/skillmanager). (5) git show 8e6c0b49 = the onionmorph VENDOR commit (2026-07-08): persona tokens in its diff are ONLY in +++ HEAVEN-DOTDIR-SPEC.md; it also added onionmorph/lib/persona.py (269L) but that is onionmorph's own persona lib, no CLAUDE.md-key parsing. (6) git show 262f4cf9 persona lines = a QUOTED journal FINDING about the skill-manager equip_persona/carton_identity model (a DIFFERENT persona system), NOT devdir keys = red herring. NET: the closest RUNNING code = heaven dir_context.py (loads dir rules, single-active, swap-on-move) which onionmorph wires; the persona-KEY resolution (skillmanager_persona=/absolute_) is Phase-2-ACCEPTED-not-built. Isaac's memory we-did-this-in-heaven = the DESIGN was done + it IS about heaven system-prompt resolution (correct instinct) but implementation was never written = classic ACCEPTED-Phase-2 remembered as done (state-what-is-vs-vision). RECONCILIATION: my session heaven work (baseheavenagent.py working_dir + _track_active_work_dir single-active-swap + _resolve_devdir_roots + two-phase _devdir_levels cfc17e29) is a PARALLEL SECOND implementation of the SAME read/bash-tracked dir-rule-loading that dir_context.py already does -- TWO mechanisms now. The MISSING piece per the spec = persona-key (skillmanager_persona=/absolute_) resolution in heaven, on top of whichever loader wins. FIX not yet designed -- Isaac fork on which loader + reconcile. No backticks.  tags:[Devdir_Resolution]
- [v6]  2026-07-09T05:17:46  DECISION: --tag Devdirs CORRECT MODEL of heaven devdir + persona resolution (Isaac confirmed 2026-07-09, verbatim semantics). The system is about AUTOMATIC context loading (MCPs are MANUAL and NOT part of this; SkillManager is only the STORE a forced persona comes from). ALL of it lives INSIDE BaseHeavenAgent (never dir_context.py, which is a wrong-place duplicate to retire). TWO KINDS OF LOADING, both automatic, both triggered by what is in the loaded prompt text; the difference is PERSISTENCE ON MOVE: (1) AMBIENT DEVDIR RULES = .claude/.heaven/CLAUDE.md/AGENTS.md text, location-based, EPHEMERAL = SWAPS as the agent moves (leave a dir -> its rules drop), walks UP the parent chain (NOT .git-bounded: enter to nearest devdir-bearing ancestor, climb while parent also has one). (2) PERSONA = declared by skillmanager_persona=X or absolute_skillmanager_persona=X appearing in ANY surface it loads (a CLAUDE.md OR AGENTS.md OR the heaven system prompt). A persona = {frame OVERRIDES the system prompt, skillset OVERRIDES skills, mcpset OVERRIDES mcps} + a carton COLLECTION = the carton identity. DEFINING PROPERTY: PERSONA DOES NOT GET LOST WHEN THE AGENT MOVES -- it is FORCED and STICKY, once picked up it stays on the agent PERMANENTLY everywhere it goes, UNLIKE ambient rules which swap. TWO FORMS: skillmanager_persona=X (FORCE) = forced+sticky, still COMPOSES WITH PARENTS; absolute_skillmanager_persona=X (ABSOLUTE) = forced+sticky AND IGNORES PARENTS (severs the parent devdir walk; only this persona). THE TWOFOLD FIRING (Isaac): (a) WHEN THE SYSTEM PROMPT IS MADE -> load all this into the prompt; (b) WHEN A TOOL IS USED -> AFTER_TOOL_CALL tracks the dir the agent is looking at, then the system-prompt hook loads that dir stuff into the prompt BEFORE the agent is shown the tool result. CODE SHAPE NEEDED (distinct from the swapping _active_work_dir): a STICKY persona slot on the agent (set once when skillmanager_persona= seen in loaded text, NEVER cleared on move = what makes it not-get-lost), an ABSOLUTE flag that makes resolve_devdirs skip the parent/ambient walk, and applying the persona = frame overrides the system prompt resolve_devdirs produces + skillset override. No backticks.  tags:[Devdir_Resolution]
